{
  "name": "highlight-selected",
  "author": "lumine-code",
  "description": "Highlight all occurrences of the selected text.",
  "repository": "https://github.com/lumine-code/highlight-selected",
  "bugs": {
    "url": "https://github.com/lumine-code/highlight-selected/issues"
  },
  "keywords": [
    "occurrences",
    "word",
    "mark",
    "navigation",
    "match"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "{% if keys['highlight-selected:go-to-next'] %}You can jump to the next occurrence of the selected text with {{ 'highlight-selected:go-to-next' | keystroke }}{% else %}Selecting a word highlights every other occurrence of it in the file.{% endif %}"
  ],
  "main": "./lib/main",
  "files": [
    "docs",
    "lib",
    "menus",
    "spec",
    "styles"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "providedServices": {
    "highlight-selected": {
      "description": "Exposes highlight selection events",
      "versions": {
        "1.0.0": "provideHighlightSelected"
      }
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.3.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6"
  },
  "configSchema": {
    "minimumLength": {
      "title": "Minimum Length",
      "description": "Minimum number of characters in selection before highlights are shown",
      "type": "integer",
      "default": 2
    },
    "maximumHighlights": {
      "title": "Maximum Highlights",
      "description": "For performance purposes, the number of highlights is limited. Set to 0 for unlimited.",
      "type": "integer",
      "minimum": 0,
      "default": 0
    },
    "timeout": {
      "title": "Debounce Timer",
      "description": "Debounce timer in ms before searching; resets on each selection change",
      "type": "integer",
      "default": 20
    }
  }
}
